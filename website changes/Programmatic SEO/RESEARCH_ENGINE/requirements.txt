# Agentic AI Labs — pSEO Research Engine
# Install with: pip install -r requirements.txt

requests>=2.31.0          # HTTP calls to BLS, Reddit, job search
feedparser>=6.0.11        # RSS feed parsing for news nodes
pytrends>=4.9.2           # Google Trends (unofficial wrapper)
beautifulsoup4>=4.12.0    # HTML parsing for job market scraping
lxml>=5.1.0               # Fast XML/HTML parser (feedparser + bs4 backend)
